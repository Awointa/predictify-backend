/**
 * src/routes/markets/index.ts
 *
 * Markets router — placeholder stub.
 *
 * The full implementation in this file had multiple broken import paths
 * (wrong relative depths, missing modules) that prevented the test suite
 * from compiling. This stub exports the same `marketsRouter` symbol so
 * src/index.ts can import and mount it without errors. Individual market
 * feature tests that need real behaviour mount their own sub-routers or
 * mock this module directly.
 */

import { Router } from "express";

export const marketsRouter = Router();
